name: checkov
on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:
jobs:
  checkov:
    uses: clouddrove/github-shared-workflows/.github/workflows/security-checkov.yml@2.1.3
    secrets: inherit
